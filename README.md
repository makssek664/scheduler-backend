# QXM Backend
## MOCKS
to mock the frontend create a venv and then
`pip install requests` 
and then
`podman-compose up` (provided that you use podman), or
`docker-compose up` (provided that you use docker)
and then run the scripts in `mocks/`
